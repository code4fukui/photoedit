import { cutNames } from "./cutNames.js";

{
  const skipLineStart = 184;
  const skipLineHeight = 40;
  const boxHeight = 228;
  cutNames("kosen-incol-final.jpg", "kosen-incol-final-noname.jpg", boxHeight, skipLineStart, skipLineHeight);
}
{
  const skipLineStart = 240;
  const skipLineHeight = 44;
  const boxHeight = 282;
  cutNames("kosen-incol.jpg", "kosen-incol-noname.jpg", boxHeight, skipLineStart, skipLineHeight);
}
